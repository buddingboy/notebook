from django.conf.urls import url
from api import views, serializers

urlpatterns = [
    # url(r'^tasks/$', serializers.NoteListView, name='task_list'),
    # url(r'^tasks/(?P<pk>[0-9]+)$', views.task_detail, name='task_detail'),

    ]